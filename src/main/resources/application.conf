ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ demircandemir.com.ApplicationKt.module ]
    }
}

database {
    host = "localhost"
    port = "5432"
    name = "stylishdatabase"
    user = "postgres"
    password = "1852"
    maxPoolSize = 3
}