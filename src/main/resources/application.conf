ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ demircandemir.com.ApplicationKt.module ]
    }
}

database {
    driverClassName = "org.postgresql.Driver"
    jdbcURL = "jdbc:postgresql://localhost:5432/stylishdatabase"
    username = "postgres"
    password = "1852"
    maxPoolSize = 3
} 